/**
 * Displays message on STDOUT.
 * @param {String} msg 
 * @author Alvin Gichuhi <https://github.com/mliooh>
 */
const displayMessage = (msg) => {
  console.log(msg);
};

module.exports = displayMessage;